namespace GcLib;

/// <summary>
/// Utility class, holding references to constants used in the library.
/// </summary>
public static class Constants
{
    /// <summary>
    /// Value indicating an infinite number (e.g., timeout or number of buffers).
    /// </summary>
    public const ulong GENTL_INFINITE = 0xFFFFFFFFFFFFFFFF;
}